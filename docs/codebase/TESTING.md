# Testing Patterns

## Core Sections (Required)

### 1) Test Stack and Commands

- Primary test framework: Vitest 4 through Angular's
  `@angular/build:unit-test` builder.
- Assertion/mocking tools: Vitest `expect`, Angular `TestBed`,
  `HttpTestingController`, `provideHttpClient`, and
  `provideHttpClientTesting`.
- Commands:

```bash
npm test
npm test -- --watch=false
npm test -- --watch=false --include src/app/app.spec.ts
```

- There is no configured coverage command or threshold.

### 2) Test Layout

- Test file placement: co-located with the root component in
  `src/app/app.spec.ts`.
- Naming convention: `*.spec.ts`.
- Test setup is local to the suite's `beforeEach`; there is no repository-wide
  setup file shown in the source tree.
- The suite imports the standalone `App` component and configures providers
  through `TestBed`.

### 3) Test Scope Matrix

| Scope | Covered? | Typical target | Notes |
|-------|----------|----------------|-------|
| Unit | Partial | Root signals and rendered component behavior | Assertions are primarily integration-style DOM checks |
| Integration | Yes | App + `TeamDataService` HTTP boundary | Request is intercepted and sample `TeamData` is flushed |
| E2E | No | Browser navigation and responsive layout | `[TODO]` Add only if the project needs browser-level regression coverage |

The current suite covers creation, loaded bilingual content, language switching,
event ordering/media fields, and teardown verification.

### 4) Mocking and Isolation Strategy

- Main mocking approach: `HttpTestingController` intercepts the relative
  `data/team-data.json` request; no real network call is made.
- Isolation guarantees: each test configures a fresh `TestBed`; `afterEach`
  calls `http.verify()` to detect unexpected requests.
- Common failure mode: the suite depends on the exact relative data URL and on
  Angular fixture stabilization before DOM assertions.

### 5) Coverage and Quality Signals

- Coverage tool + threshold: `[TODO]` no coverage tool or threshold is
  configured.
- Current reported coverage: `[TODO]` not generated by existing commands.
- Known gaps: no E2E, accessibility automation, visual regression, schema
  validation suite, or test step in the deployment workflow.

### 6) Evidence

- `package.json`
- `angular.json`
- `src/app/app.spec.ts`
- `src/app/services/team-data.service.ts`
- `.github/workflows/deploy.yml`
